{
  "$schema": "https://schema.tauri.app/config/capability",
  "identifier": "default",
  "description": "Default security capabilities for AburMD",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default"
  ]
}
